APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

# Admin: comma-separated emails that may access /admin without is_admin=true (bootstrap only).
# ADMIN_EMAILS=you@example.com

# SEO: canonical base for sitemaps, robots, and JSON-LD (defaults to APP_URL).
# SEO_BASE_URL=https://www.example.com
# SEO_ORG_NAME=LexiCraft
# SEO_ORG_URL=https://www.example.com
# SEO_ORG_LOGO_URL=https://www.example.com/og/logo.png
# SEO_ORG_DESCRIPTION=
# SEO_SITEMAP_CONCEPTS_PER_FILE=2000
# SEO_SITEMAP_DOMAINS_PER_FILE=5000

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=lexicraft
DB_USERNAME=postgres
DB_PASSWORD=

# Optional: SQLite for quick experiments or when PHPUnit uses file DB (tests default to in-memory SQLite via phpunit.xml).
# DB_CONNECTION=sqlite
# DB_DATABASE=database/database.sqlite

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# Laravel Scout (set to meilisearch in production with a running Meilisearch instance)
SCOUT_DRIVER=collection
SCOUT_PREFIX="${APP_NAME}_"
SCOUT_QUEUE=false

MEILISEARCH_HOST=http://127.0.0.1:7700
MEILISEARCH_KEY=
